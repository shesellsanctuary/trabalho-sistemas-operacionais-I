#ifndef UTILS_H
#define UTILS_H

#include "support.h"
#include "EOperationStatus.h"

// Searches queue for the value inside content
EOperationStatus SearchFila2(PFILA2 queue, void* content);

#endif // !UTILS_H