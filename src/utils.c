#include <stdlib.h>
#include "utils.h"

// Searches queue for the content of content
EOperationStatus SearchFila2(PFILA2 queue, void* content)
{
	// Operation status
	EOperationStatus retStatus = OpUknownError;

	// If allocation was successfull
	if (queue != NULL)
	{

	}

	return retStatus;
}